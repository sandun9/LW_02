package Q_01;

public class Expression {

        public static void main(String[] args) {
            double A,B,C,X,Y;
            A=5; //INICIALICE RANDOM NUMBER
            B=8; //INICIALICE RANDOM NUMBER
            C=3;  //INICIALICE RANDOM NUMBER
            X=2;  //INITIALICE RANDOM NUMBER
            Y=1;  //INITIALICE RANDOM NUMBER
            int radious;
            radious=2; //initialice random value

            double result1=Math.sqrt(B*B+4*A*C);
            double result2=Math.sqrt(X*4*Math.pow(Y,3));
            double result3=Math.cbrt(X*Y);
            double result4=Math.PI *Math.pow(radious,2);
        }
    }

